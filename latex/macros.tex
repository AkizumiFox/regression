% Book-wide notation for "Regression: From Projection to Distribution".
% Used by the PDF (included in the LaTeX preamble) and by the website (converted to
% MathJax macros by build/macros.py). See authoring/NOTATION.md for the conventions.
% Only \def, \newcommand, \providecommand and \DeclareMathOperator are understood by the
% web converter. Operators the engine's LaTeX style already defines use \providecommand,
% so the PDF keeps the engine's definition and the website gets the same one from here.

% PDF only: the engine's style (latex-template.sty) already defines several of these names
% (\R and \C as blackboard letters, \T as an upright bold T, ...), which made the \newcommand
% lines below fail silently. Clearing them first lets this file's definitions win. The web
% converter ignores \let.
\let\A\relax \let\B\relax \let\C\relax \let\E\relax \let\G\relax \let\I\relax
\let\M\relax \let\Q\relax \let\R\relax \let\T\relax \let\V\relax \let\W\relax
\let\X\relax \let\Y\relax \let\Z\relax \let\norm\relax

% Bold vectors and matrices are bold UPRIGHT, Latin and Greek alike. Latin: \mathbf{a}.
% Greek: \boldsymbol{\uptheta} (upright Greek from upgreek; Euler in the PDF, since Computer
% Modern has no upright lowercase Greek; MathJax loads its own upgreek extension). Capital Greek
% is upright already: \boldsymbol{\Sigma}. \bm is kept only so older text compiles; do not use it.
% The engine's style loads upgreek (with Euler, which is upgreek's default) before this file;
% asking for it again here only raised "Option clash for package upgreek" and changed nothing.
\newcommand{\vect}[1]{\mathbf{#1}}
\providecommand{\bm}[1]{\boldsymbol{#1}}
\newcommand{\Y}{\vect{Y}}
\newcommand{\y}{\vect{y}}
\newcommand{\X}{\vect{X}}
\newcommand{\x}{\vect{x}}
\newcommand{\W}{\vect{W}}
\newcommand{\Z}{\vect{Z}}
\newcommand{\bb}{\vect{b}}
\newcommand{\bv}{\vect{v}}
\newcommand{\bu}{\vect{u}}
\newcommand{\bw}{\vect{w}}
\newcommand{\bz}{\vect{z}}
\newcommand{\br}{\vect{r}}
\newcommand{\be}{\boldsymbol{\upvarepsilon}}
\newcommand{\bbeta}{\boldsymbol{\upbeta}}
\newcommand{\bgamma}{\boldsymbol{\upgamma}}
\newcommand{\bmu}{\boldsymbol{\upmu}}
\newcommand{\blambda}{\boldsymbol{\uplambda}}
\newcommand{\bzero}{\vect{0}}
\newcommand{\bone}{\vect{1}}
\newcommand{\I}{\vect{I}}
\newcommand{\A}{\vect{A}}
\newcommand{\B}{\vect{B}}
\newcommand{\M}{\vect{M}}
\newcommand{\Mo}{\vect{M}_0}
\newcommand{\Q}{\vect{Q}}
\newcommand{\R}{\vect{R}}
\newcommand{\V}{\vect{V}}
\newcommand{\G}{\vect{G}}
\newcommand{\bP}{\vect{P}}
\newcommand{\bH}{\vect{H}}
\newcommand{\bL}{\vect{L}}
\newcommand{\bD}{\vect{D}}
\newcommand{\bS}{\vect{S}}
\newcommand{\bT}{\vect{T}}
\newcommand{\bU}{\vect{U}}
\newcommand{\bSigma}{\boldsymbol{\Sigma}}
\newcommand{\bLambda}{\boldsymbol{\Lambda}}
\newcommand{\hbeta}{\hat{\bbeta}}
\newcommand{\hY}{\hat{\Y}}
\newcommand{\he}{\hat{\be}}

% Spaces and operators
\newcommand{\Real}{\mathbb{R}}
\newcommand{\C}{\mathcal{C}}
\newcommand{\Null}{\mathcal{N}}
\newcommand{\T}{^{\top}}
\newcommand{\ginv}{^{-}}
\newcommand{\perpc}{^{\perp}}
\providecommand{\rank}{\operatorname{rank}}
\providecommand{\tr}{\operatorname{tr}}
\providecommand{\diag}{\operatorname{diag}}
\providecommand{\spn}{\operatorname{span}}
\providecommand{\Var}{\operatorname{Var}}
\providecommand{\Cov}{\operatorname{Cov}}
\DeclareMathOperator*{\argmin}{arg\,min}
\newcommand{\vecop}{\operatorname{vec}}
\newcommand{\E}{\operatorname{E}}
\newcommand{\norm}[1]{\lVert #1\rVert}
\newcommand{\inner}[2]{\langle #1,#2\rangle}
\newcommand{\iid}{\overset{\text{iid}}{\sim}}
\newcommand{\Normal}{\mathrm{N}}
\newcommand{\dirsum}{\oplus}

% PDF only: the engine redefines single lower-case letters (\x, \y, ...) as upright bold
% vectors at \begin{document}; restate the book's bold italic versions after it.
\AtBeginDocument{\def\x{\vect{x}}\def\y{\vect{y}}}
