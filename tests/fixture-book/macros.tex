% Macros for the fixture book
\def \nR {\ensuremath{\mathbbmss{R}}}
\providecommand{\norm}[1]{\left\| #1 \right\|}
% A name the book template does not already declare (\rank is declared there,
% so redeclaring it is a LaTeX error the PDF gate rightly rejects)
\DeclareMathOperator{\codim}{codim}
